# Spine chibi assets

Tài nguyên animation của 32 nhân vật dùng trong phần xem trước Backgear.

Mỗi thư mục nhân vật gồm skeleton Spine 3.8 (`.json`), texture atlas (`.atlas`) và một hoặc nhiều texture WebP.

Danh sách đường dẫn được quản lý tại `src/data/backgearChibis.json`.
