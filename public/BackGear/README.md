# Backgear assets

Thư mục chứa tài nguyên cho trang `/backgear`:

- Thư mục `optimized` chứa 9 ảnh WebP hiển thị cùng thumbnail để tải và chuyển nền nhanh hơn.

Đường dẫn ảnh hiển thị và thumbnail được khai báo trong `src/data/backgear.json`.
